<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="evonative.hilt.coroutines.retrofit.example" >

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="com.evonative.vodworks.test.VodWorksApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Hilt.coroutines.retrofit.example"
        android:usesCleartextTraffic="true" >
        <activity
            android:name="com.evonative.vodworks.test.presentation.activity.ItemDetailActivity"
            android:exported="true" />
        <activity
            android:name="com.evonative.vodworks.test.presentation.activity.HomeActivity"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>